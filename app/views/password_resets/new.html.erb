<h1>Reset Your Password</h1>

<%= form_with url: password_reset_path do |form| %>
  <div>
    <%= form.label :email %>
    <%= form.password_field :email %>
  </div>

  <div>
    <%= form.submit "Reset Password" %>
  </div>
<% end %>
